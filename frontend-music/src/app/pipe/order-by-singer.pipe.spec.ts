import { OrderBySingerPipe } from './order-by-singer.pipe';

describe('OrderBySingerPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBySingerPipe();
    expect(pipe).toBeTruthy();
  });
});
